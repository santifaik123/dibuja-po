# Dibuja Po - Railway single-server deploy

# Railway injects PORT automatically. Use this only for local custom ports.
PORT=3000

# Optional in production if the frontend is served from a different origin.
# For the recommended Railway single-server deploy, leave empty because
# Next.js and Socket.io share the same origin.
# Comma-separated values are supported.
CLIENT_URL=
